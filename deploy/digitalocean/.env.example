APP_DOMAIN=toonabi.example.com
LETSENCRYPT_EMAIL=ops@example.com

RAILS_MASTER_KEY=replace_me
# Optional here if already present in Rails credentials.
ANTHROPIC_API_KEY=

TTS_PROVIDER=fish
# Optional here if already present in Rails credentials.
FISH_AUDIO_API_KEY=
FISH_AUDIO_MODEL=speech-1.6
# Optional here if already present in Rails credentials.
ELEVENLABS_API_KEY=
ELEVENLABS_MODEL=eleven_multilingual_v2

RAILS_LOG_LEVEL=info
WEB_CONCURRENCY=1
RAILS_MAX_THREADS=3

# Leave blank when the frontend and backend share the same domain via Caddy.
VITE_VRM_BASE_URL=
